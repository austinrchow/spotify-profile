import React, { useState, useEffect } from "react";
import styled from "styled-components";

const Container = styled.div`
  display: flex;
  background-color: #181818;
  height: 100vh;
  width: 100vw;
`;
const TopArtists = () => {
  return <Container>Top Artists</Container>;
};

export default TopArtists;
